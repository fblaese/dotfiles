# start ssh agent
killall -q ssh-agent; while pgrep -u $UID -x ssh-agent >/dev/null; do sleep 1; done;
eval `ssh-agent`

# firefox native scrolling
export MOZ_USE_XINPUT2=1
# vlc scaling (qt5)
export QT_AUTO_SCREEN_SCALE_FACTOR=0
